version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Building the project...
      - npm run build

  post_build:
    commands:
      - echo Starting the application...
      - npm run start:prod

artifacts:
  files:
    - dist/**     
    - package.json 
    - .env          
  discard-paths: no

env:
  variables:
    NODE_ENV: "production"
